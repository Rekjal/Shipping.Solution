@using Shipping.Models;

<h1>To Do List</h1>

@if (Model.Count == 0)
{
  <p>There are no items in the list.</p>
}
<ul>
  @foreach (Parcel newParcel in Model)
  {
    <li>@newParcel.Height, @newParcel.CalcVolume() </li>
  }
</ul>
<a href="/parcels/new">Add a new item.</a>